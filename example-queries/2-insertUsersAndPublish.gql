# Write your query or mutation here
mutation insertUsersAndPublish {
  insert_users_and_publish(name: "JYC", rocket: "codegen") {
    affected_rows
    returning {
      id
      name
      rocket
    }
  }
}
